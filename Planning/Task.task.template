# Task Template

---

## Delegation Prompt

---

Use 4-backtick code block for delegation when handing off between sessions (see [Framework/Delegation.instructions.md](../Ai/Framework/Delegation.instructions.md)). Model designation is optional in this local setup.

````markdown
**Task**: [{Slice}/{TaskName}.task]({Slice}/{TaskName}.task)
**Role**: {Role} (see [Framework/{Prompt}.prompt.md](../Ai/Framework/{Prompt}.prompt.md))
**Target Audience**: {Agent}

{1-2 sentence context}
````

## Role: {Orchestrator|Planner|Reviewer|Implementer|Scaffolder|Cleanup}

**Task File**: {Slice}/{TaskName}.task
**Objective**: {One-line objective}

**Role Reference**: See [Framework/{Role}.prompt.md](../Framework/{Role}.prompt.md) for workflow guidance.

### Context

{Why this task exists and what it unlocks}

### Your Responsibilities

1. {Responsibility 1}
2. {Responsibility 2}

### Deliverables

- [ ] **D1**: {Acceptance criteria}
- [ ] **D2**: {Acceptance criteria}

### Implementation Guidance (optional)

**Patterns to Follow**: {Links or file paths in host repo}

#### Verification Steps

- [ ] `<BUILD_CMD>` succeeds (0 errors, 0 warnings)
- [ ] `<TEST_CMD>` passes
- [ ] `<LINT_CMD>` / `<DOC_LINT_CMD>` clean if applicable

**Critical Notes**: {Any must-follow constraint}

### Reports

- Keep status/blocker reports in separate `.report` files named `{TaskName}.task.{ReportName}.report` alongside this task file
- Reference the report filenames here (do not inline full reports)
- Move reports together with the task when archiving/moving
- Use [Planning/ProgressReport.report.template](../Planning/ProgressReport.report.template) for status updates and [Planning/BlockerReport.report.template](../Planning/BlockerReport.report.template) when blocked

### Completion Delegation

1. Update Progress Log in this file (summaries only; link to `.report` files for details).
1. Delegate to the next role using the delegation code block when required:

````markdown
**Task**: [{TaskPath}]({TaskPath})
**Role**: {NextRole} (see [Framework/{NextPrompt}.prompt.md](../Framework/{NextPrompt}.prompt.md))
**Target Audience**: {NextAgent}

{Brief context}
````

1. When complete, archive to `{Slice}/archive/{TaskName}.YYYY-MM-DD.task` and update relevant inventories.

---

## Task Details

---

Status: {Not Started|In Progress|Complete|Blocked}
Owner: {Role}
Priority: {Critical|High|Medium|Low}
Effort: {Small|Medium|Large}
Priority Score: {1-15} (see [Planning/TaskPrioritySystem.md](TaskPrioritySystem.md))

## Objective

{Detailed objective}

## Task Context

- Current state: {bullets}
- Desired state: {bullets}

## Task Deliverables

- D1: {Description and acceptance}
- D2: {Description and acceptance}

## Architecture Decisions

- AD-1: {Decision + rationale}
- AD-2: {Decision + rationale}

## Success Criteria

- [ ] `<BUILD_CMD>` succeeds (0 warnings)
- [ ] `<TEST_CMD>` passes
- [ ] `<LINT_CMD>`/`<DOC_LINT_CMD>` clean if applicable
- [ ] Task-specific checks

## Progress Log

- YYYY-MM-DD: {Note}
